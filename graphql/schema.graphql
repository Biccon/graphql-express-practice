type User {
  id: String!
  pw: String!
  nickname: String!
}

type Article {
  writerId: String!
  title: String!
  content: String!
}

type Query {
  me: User
}

type Mutation {
  login(id: String!, pw: String!): String
}
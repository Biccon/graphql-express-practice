type User {
  id: String!
  nickname: String
  admin: Boolean
  email: String
}

type Article {
  writerId: String!
  title: String!
  content: String!
}

type Query {
  me: User
}

type Mutation {
  login(id: String!, pw: String!): String
}

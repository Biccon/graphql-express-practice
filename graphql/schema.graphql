type User {
  id: String!
  pw: String!
  nickname: String!
}

type Article {
  writerId: String!
  title: String!
  content: String!
}

type Query {
  me: User
  myArticles: [Article]
}

type Mutations {
  register(id: String!, pw: String!): User
  login(id: String!, pw: String!): String
}